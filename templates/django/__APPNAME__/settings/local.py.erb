"""
Local Settings
"""
from .development import *

<%
    o = [('a'..'z'), ('A'..'Z'), ('0'..'9'), "!@#$%^&*():".split('')].map { |i| i.to_a }.flatten
    randkey = (0...50).map { o[rand(o.length)] }.join
%>

SECRET_KEY = '<%= randkey %>'
