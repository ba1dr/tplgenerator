
```
sudo ln -s /opt/<%= @project_name %>/config/nginx/<%= @project_name %>.conf /etc/nginx/sites-available/
sudo ln -s /etc/nginx/sites-available/<%= @project_name %>.conf /etc/nginx/sites-enabled/
sudo sh -c 'printf "admin:$(openssl passwd -noverify -apr1)\n" > /etc/nginx/<%= @project_name %>_htpasswd'
```